<div style="overflow-y: auto;">
    {{~it.tracks :track}}
        <input type="radio" name="track" value="{{!track.aid}}">{{!track.artist}} — {{!track.title}}</input>
        <br />
    {{~}}
</div>
