<tr>
    <td colspan="{{ out += (it.post.fileInfos.length + 2); }}">
        <div style="max-width: 90%;">
            <font face="monospace">{{ out += (new Array(50)).join("-"); }}<br />{{=it.userAgent}}</font>
        </div<
    </td>
</tr>
